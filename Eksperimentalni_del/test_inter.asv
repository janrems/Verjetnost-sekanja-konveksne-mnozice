t = 0:0.01:2*pi;
rezult=zeros(length(t), 2);
a=2.*rand(1);
b=3.*rand(1);
pmax = max(a,b);
am = a*rand(1);
bm = b*rand(1);
rezultm=zeros(length(t), 2);
for i=1:length(t)
rezult(i, 1)=a*cos(t(i));
rezult(i, 2)=b*sin(t(i));
end
plot(rezult(:,1),rezult(:,2))

t= [[-3 3];[-3 3];[-3 3]];
z = [[-3 3]; [5 6]; [-2 2]];
n_pres = sum((length(intersections(t(,z,a(h),b(h)));