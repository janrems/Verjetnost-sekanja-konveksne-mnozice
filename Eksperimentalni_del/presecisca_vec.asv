function [n] = presecisca_vec(A,velika)
    l = length(A(:,1));
    x1 = A(:,1)
    x2 = A(:,3)
    y1 = A(:,2)
    y2 = A(:,4)
    velikax = velika(:,1);
    velikay = velika(:,2); 
    a=[a1; a2];
    a3=a3(:)'; 
end