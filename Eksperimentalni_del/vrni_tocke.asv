function [A] = vrni_tocke(n,pmax)
    B = skupaj(n,pmax);
    y1 = -pmax .*B(:,1) + B(:,2)
    y2 = pmax .*B(:,1) + B(:,2)
    x1 = -pmax * ones(n,1);
    x2 = pmax * ones(n,1);
    A = [x1 y1 x2 y2];
    plot(A(1,1:2,y2(2))
end
        